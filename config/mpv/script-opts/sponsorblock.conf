# SponsorBlock (po5 mpv script) --- LIVE skipping for STREAMED YouTube URLs only
# (search/preview/emergency). Downloaded files carry SB segments as embedded chapters that
# scripts/sponsorskip.lua skips offline, so make_chapters is off here (no chapter clutter).
categories=sponsor,intro,outro,interaction,selfpromo,filler
skip_categories=sponsor,intro,outro,selfpromo,interaction
make_chapters=no
