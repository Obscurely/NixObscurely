#!/usr/bin/env bash
# All closing is universal now (unbind keyset + close every popup + backdrop + cava). Kept as a
# thin alias so launcher-run / older callers keep working.
exec bash "$HOME/.config/eww/scripts/popup-close"
